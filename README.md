# Contingent-Workforce-Rate-Benchmarking
This defines a Python-based rate benchmarking agent that scrapes public salary/job data, normalises roles, converts salaries to daily contractor rates (local currency), generates percentile bands, detects market trends, stores results in SQLite, exports rate-card-ready CSVs, and runs in Docker with modular, testable code.
